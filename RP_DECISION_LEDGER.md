# RP_DECISION_LEDGER — Append-only

File-based until the DB-backed ledger (M1 core) is live; entries then migrate in. Format: ID | date | decision | rationale | actor.

- **D-001** | 2026-07-11 | LMS RBAC via `users.role` enum (student/instructor/staff/admin), checked server-side per request, roles never settable via OIDC upsert or bulk import; first admin via `ADMIN_BOOTSTRAP_EMAILS`; grants via admin-only endpoint. | Cheapest correct fix for the critical PII exposure; per-request DB lookup makes revocation immediate. | Claude (M0), per canonical docs.
- **D-002** | 2026-07-11 | Site admin auth: SHA-256 + timing-safe compare of `ADMIN_PASSWORD_SHA256` (plaintext env accepted with warning), 5/min/IP rate limit; exec bible behind HTTP Basic using the same secret, file moved out of the static build to `internal/`. | Full session auth is M1 (unified identity); this closes plaintext/timing/enumeration holes without breaking the existing admin UI client. | Claude (M0).
- **D-003** | 2026-07-11 | In-house in-memory rate limiter (site) and AI metering (LMS) instead of new dependencies or Redis. | Constitution: simplest working implementation; limitation (reset on restart) documented in code and verification record; durable versions land on the M1 job/observability spine. | Claude (M0).
- **D-004** | 2026-07-11 | Removed simulated payment path entirely (`processPayment` fails loudly; route 501; bulk-enroll creates explicit $0 admin-granted enrollments). Docs corrected to say payments are NOT implemented. | VERIDIAN: no simulated production behavior; fake $8,500 transactions were a trust/compliance liability and inflated WIOA funding numbers. | Claude (M0).
- **D-005** | 2026-07-11 | LMS DB driver selectable by hostname: node-postgres for localhost, neon-http otherwise, logged at boot. | Neon serverless driver cannot reach local Postgres; local runtime verification is mandatory (runtime is the proof) and must exercise the real schema. | Claude (M0).
- **D-006** | 2026-07-11 | Raffle entry codes generated with `crypto.randomInt`; donation receipts carry "no goods or services" IRS language; raffle emails state non-deductibility; API response bodies no longer logged. | Integrity of money-linked drawing; IRS receipt compliance; PII must not leak into logs. | Claude (M0).
- **D-007** | 2026-07-11 | `rpcourses` remains a nested git repo through M0; monorepo consolidation is the first M1 step. | M0 is stop-the-bleeding only; repo restructuring mid-hotfix would mix concerns and complicate rollback. | Claude (M0).
- **D-008** | 2026-07-11 | Program architecture: V.I.A. (AI-first online school), N.O.B.L.E. (youth leadership/mentorship community), and Workforce Development (CompTIA/CNA/WIOA) are independent program types sharing the M1 core (identity, events, registry, graph) — not separate applications and not folded into each other. | Owner directive (this session); matches RP_MASTER_ARCHITECTURE one-spine/many-surfaces thesis. | Owner + Claude (M1 scoping).
